import React from 'react';

const Search = () => {
    return (
        <form>
            <input type={'text'} placeholder={'search'} />
            <button>search</button>
        </form>
    );
};

export {Search};